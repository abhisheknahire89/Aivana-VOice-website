# Required for REST voice; also set for server (Gemini Live).
VITE_GEMINI_API_KEY=your_gemini_api_key

# Gemini Live: set one of these so the app uses Live instead of REST. Run "npm run dev:server" for local.
# VITE_VOICE_WS_URL=ws://localhost:3001/voice
# VITE_VOICEBOT_WS_URL=wss://your-voicebot.com/browser-voice
