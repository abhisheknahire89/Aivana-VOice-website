# Deploy from this directory: railway link && railway up
# Set GEMINI_API_KEY (or VITE_GEMINI_API_KEY) in Railway project variables.
[build]
builder = "nixpacks"

[deploy]
startCommand = "npm run build && npm start"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
